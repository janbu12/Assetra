{
  "name": "assetra",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "db:generate": "pnpm --filter @assetra/api prisma:generate",
    "db:migrate": "pnpm --filter @assetra/api prisma:migrate",
    "db:seed": "pnpm --filter @assetra/api prisma:seed"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  }
}
