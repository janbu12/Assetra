'use client';
import { AppShell } from '@/components/app-shell';import { Save } from 'lucide-react';import { useState } from 'react';
export default function Page(){const [saved,setSaved]=useState(false);return <AppShell><div className="content"><div className="heading-row"><div><div className="eyebrow">Sistem</div><h1 className="page-title">Pengaturan</h1><p className="subtitle">Preferensi organisasi, notifikasi, dan tampilan operasional.</p></div></div><form className="card" style={{maxWidth:720}} onSubmit={e=>{e.preventDefault();setSaved(true)}}><div className="card-title">Profil organisasi</div><div className="field"><label>Nama organisasi</label><input defaultValue="Perusahaan Demo Assetra"/></div><div className="field"><label>Zona waktu</label><select defaultValue="Asia/Jakarta"><option>Asia/Jakarta</option></select></div><div className="field"><label>Mata uang</label><select defaultValue="IDR"><option>IDR — Rupiah Indonesia</option></select></div><div className="field"><label><input type="checkbox" defaultChecked style={{width:'auto',marginRight:8}}/>Aktifkan notifikasi garansi dalam aplikasi</label></div><button className="button"><Save size={15}/>Simpan pengaturan</button>{saved&&<span className="trend" style={{marginLeft:12}}>Tersimpan di perangkat ini.</span>}</form></div></AppShell>}
