import { AppShell } from '@/components/app-shell';import { Download, FileSpreadsheet, FileText } from 'lucide-react';
export default function Page(){return <AppShell><div className="content"><div className="heading-row"><div><div className="eyebrow">Analitik</div><h1 className="page-title">Laporan</h1><p className="subtitle">Unduh data operasional untuk analisis dan kebutuhan audit.</p></div></div><div className="stats-grid"><Report icon={<FileSpreadsheet size={20}/>} title="Inventaris aset" detail="Kode, serial, lokasi, status, kondisi, dan nilai aset." href="/api/v1/reports/assets.xlsx" label="Excel"/><Report icon={<FileText size={20}/>} title="Hasil audit" detail="Daftar ditemukan, hilang, rusak, dan berbeda lokasi." href="/audit" label="Pilih sesi"/><Report icon={<FileText size={20}/>} title="Maintenance" detail="Riwayat tindakan, teknisi, waktu, dan biaya perawatan." href="/maintenance" label="Lihat data"/></div></div></AppShell>}
function Report({icon,title,detail,href,label}:{icon:React.ReactNode;title:string;detail:string;href:string;label:string}){return <div className="card"><div className="stat-icon">{icon}</div><h2 style={{font:'800 16px var(--font-display)',margin:'16px 0 7px'}}>{title}</h2><p className="subtitle" style={{minHeight:45}}>{detail}</p><a href={href} className="button secondary" style={{marginTop:16}}><Download size={14}/>{label}</a></div>}
