'use client';
import { api } from '@/lib/api';
import { ArrowRight, CheckCircle2 } from 'lucide-react';
import { useRouter } from 'next/navigation';
import { useState } from 'react';
export default function Login(){const router=useRouter();const [email,setEmail]=useState('admin@assetra.id');const [password,setPassword]=useState('Assetra123!');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});router.push('/')}catch(e){setError(e instanceof Error?e.message:'Login gagal')}finally{setBusy(false)}}return <main className="login-page"><section className="login-visual"><div className="brand" style={{margin:0}}><div className="brand-mark">A</div><span>assetra</span></div><div style={{position:'relative',zIndex:2,maxWidth:520}}><div className="eyebrow" style={{color:'#a8c0b8'}}>Satu sumber data aset</div><h1 style={{font:'800 48px var(--font-display)',letterSpacing:-2.2,lineHeight:1.08,margin:'14px 0 18px'}}>Setiap aset.<br/>Selalu terlacak.</h1><p style={{color:'#b7cbc4',lineHeight:1.8,fontSize:14}}>Kelola inventaris, audit QR, peminjaman, dan maintenance dari satu PWA yang siap dipakai di lapangan.</p><div style={{display:'grid',gap:12,marginTop:28,fontSize:12}}><span><CheckCircle2 size={16} style={{display:'inline',marginRight:8,color:'#dff36b'}}/>Audit lebih cepat dengan QR Code</span><span><CheckCircle2 size={16} style={{display:'inline',marginRight:8,color:'#dff36b'}}/>Riwayat aset lengkap dan aman</span></div></div><div style={{fontSize:10,color:'#75948a'}}>© 2026 Assetra • Manajemen aset perusahaan</div></section><section className="login-form"><form className="login-box" onSubmit={submit}><div className="eyebrow">Selamat datang</div><h2 className="page-title">Masuk ke Assetra</h2><p className="subtitle">Gunakan akun yang diberikan administrator.</p><div className="field"><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label>Kata sandi</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div>{error&&<p style={{color:'#b84b44',fontSize:11}}>{error}</p>}<button className="button" style={{width:'100%',justifyContent:'center',marginTop:8}} disabled={busy}>{busy?'Memeriksa...':'Masuk'}<ArrowRight size={15}/></button><p style={{fontSize:10,color:'#7a8782',textAlign:'center',marginTop:22}}>Demo: admin@assetra.id / Assetra123!</p></form></section></main>}
